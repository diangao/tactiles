{
  "name": "tactiles",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "selftest": "esbuild src/harness/harness.selftest.ts --bundle --platform=node --format=cjs --outfile=dist/selftest.cjs && node dist/selftest.cjs"
  },
  "devDependencies": {
    "@types/dom-speech-recognition": "^0.0.12",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@rdkit/rdkit": "^2025.3.4-1.0.0"
  }
}
