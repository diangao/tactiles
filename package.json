{
  "name": "tactiles",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app",
    "check:ops": "node scripts/check-op-sync.mjs",
    "gate": "node scripts/check-op-sync.mjs && node scripts/gate-public-artifacts.mjs"
  }
}
